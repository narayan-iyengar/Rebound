# Phase 0.5 — CoreML latency test
# Only what we need for architecture-shape export. No training deps.

torch==2.5.1              # PyTorch — architecture + ONNX export
onnx==1.17.0              # ONNX runtime for validation
coremltools==8.1          # Apple's PyTorch/ONNX → CoreML converter
numpy>=1.24,<2.0          # coremltools 8.x pins numpy <2
